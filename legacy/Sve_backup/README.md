# Backup legacy Sve.* files and directory

This directory contains the original capitalized Sve.* files moved from the repository root into a safe backup location before removing duplicates.

The files are preserved for history/comparison. Active package code lives under the lowercase `sve/` package.
